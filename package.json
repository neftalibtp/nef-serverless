{
    "name": "hello-world",
    "description": "Sample NodeJs web application",
    "version": "0.0.1",
    "private": true,
    "devDependencies": {
        "mocha": "5.0.0",
        "mocha-simple-html-reporter": "^1.1.0",
        "nyc": "^11.6.0",
        "unit.js": "2.0.0"
    },
    "scripts": {
        "test": "nyc --reporter=html mocha --reporter mocha-simple-html-reporter --reporter-options output=index.html --recursive tests"
    }
}
